import React, { Component } from "react";

//We don't need props hence function has no arguments
const Titles = () => (
    <div>
        <h1> Weather Finder</h1>
        <p> Find out temperature, conditions and more...</p>
        </div>
    );

export default Titles